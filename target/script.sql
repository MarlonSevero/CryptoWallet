DROP TABLE IF EXISTS clientes CASCADE;
DROP TABLE IF EXISTS usuarios CASCADE;
DROP TABLE IF EXISTS transacoes CASCADE;
DROP TABLE IF EXISTS receitas CASCADE;
DROP TABLE IF EXISTS cartoes_credito CASCADE;
DROP TABLE IF EXISTS gastos_cartao CASCADE;
DROP TABLE IF EXISTS carteiras CASCADE;


CREATE TABLE users (
    id int GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    cpf VARCHAR(14) UNIQUE,
    nome VARCHAR(100) NOT NULL,
    dt_nascimento DATE NOT NULL,
    renda NUMERIC(12, 2),
    email VARCHAR(100) UNIQUE NOT NULL,
    senha VARCHAR(255) NOT NULL UNIQUE
);

CREATE TABLE address (
    id int GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    rua VARCHAR(100) NOT NULL,
    bairro VARCHAR(100),
    numero VARCHAR(10),
    complemento VARCHAR(100),
    cidade VARCHAR(100) NOT NULL
);

ALTER TABLE users ADD COLUMN id_address int;
ALTER TABLE users ADD CONSTRAINT pk_address_id FOREIGN KEY(id) REFERENCES address(id);